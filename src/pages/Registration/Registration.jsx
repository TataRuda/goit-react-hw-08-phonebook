import { Helmet } from "react-helmet";

export default function Registration() {
    return (
        <div>
            <Helmet>
                <title>Registration</title>
            </Helmet>
        </div>
    );
}